name: 'f1_data_hub'
version: '1.0.0'
config-version: 2

profile: 'f1_data_hub'

model-paths: ["models"]
seed-paths:  ["seeds"]
test-paths:  ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  f1_data_hub:
    staging:
      +materialized: view
      +schema: staging
    mart:
      +materialized: table
      +schema: mart
